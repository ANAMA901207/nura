# Nura — Sistema de Aprendizaje Adaptativo
# Instalar con: pip install -r requirements.txt

# Interfaz web
streamlit==1.56.0

# Orquestación de agentes LLM
langgraph==1.1.6
langchain-google-genai==4.2.1
langchain-core==1.2.24

# Visualización del mapa de conocimiento
pyvis==0.3.2

# Variables de entorno (.env)
python-dotenv==1.2.1

# Tipos estáticos para TypedDict en Python < 3.11
typing-extensions==4.15.0

# Hashing de contraseñas
bcrypt==5.0.0

# Generación de imágenes (favicon, diagramas)
Pillow==12.0.0

# Búsqueda web desde el agente tutor
# PyPI: ddgs  →  from ddgs import DDGS
ddgs==9.13.0

# Conector PostgreSQL para Supabase (Sprint 22)
# Solo se usa cuando DATABASE_URL está definida en .env
psycopg2-binary==2.9.11


# Bot de Telegram (Sprint 25)
python-telegram-bot==21.6
fastapi==0.115.0
uvicorn==0.32.0
httpx>=0.28.1,<1.0.0

# Text-to-Speech y conversión de audio (Sprint 27)
gTTS==2.5.1        # genera MP3 desde texto sin API key
pydub==0.25.1      # convierte MP3 → OGG/OPUS para sendVoice (requiere ffmpeg)